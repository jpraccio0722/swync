---
name: line
params: [start, end, duration]
arities: [3]
variadic: false
receives: number
returns: signal
---

One straight segment: `start` to `end` over `duration` seconds,
held constant at `end` value after `duration.
