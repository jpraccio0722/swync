---
name: env
params: [attack, decay, sustain, release, duration]
arities: [5]
variadic: false
receives: number
returns: signal
---

Time-based ADSR for one-shot voices.
Attack,
decay and sustain fill `duration`. The note is held for `duration` and the voice lasts `duration + release`.
All arguments are constants.
