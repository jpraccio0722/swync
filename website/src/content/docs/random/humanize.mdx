---
name: humanize
params: [x, amount]
arities: [2]
variadic: false
receives: number
returns: number
---

Nudge a number off its grid: `x` plus a normal draw of deviation `amount`,
so most nudges are small and a few are not.
`0.5.humanize(0.05)` is a velocity that no longer sounds typed in.
