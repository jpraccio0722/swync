---
name: cauchy
params: [median, spread]
arities: [0, 1, 2]
variadic: false
receives: number
returns: number
---

A heavy-tailed draw around `median` (default 0) with the given `spread` (default 1).

