---
name: rand
params: [lo, hi]
arities: [0, 2]
variadic: false
receives: number
returns: number
---

A uniform number.
`rand()` draws from 0..1; `rand(lo, hi)` — or `60.rand(72)` — draws from `lo..hi`,
with `hi` excluded.
