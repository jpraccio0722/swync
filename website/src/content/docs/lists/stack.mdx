---
name: stack
params: [pattern]
arities: [1]
variadic: true
receives: pattern
returns: pattern
---

Creates patterns that sound at once instead of in turn: `stack([c4;2, e4;2], [g4;4])`.
Each argument keeps its own division, so the layers need not agree on a rhythm.
Creates chords and polyphony.