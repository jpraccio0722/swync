---
name: aftertouch
params: [device, channel, lo, hi]
arities: [1, 2, 4]
variadic: false
receives: number
returns: signal
---

Channel pressure — how hard the keys are being leant on — as a signal, 0 to 1.

Give a range to map it. Smoothed like `cc`. Zero on a keyboard that does not
send it.
