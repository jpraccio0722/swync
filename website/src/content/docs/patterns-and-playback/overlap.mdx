---
name: overlap
params: [play, bars, section]
arities: [3]
variadic: false
receives: play
returns: play
---

`then`,
but the section starts `bars` *before* the first one ends.
Two functions will sound together over the join: `playn(verse, lead, 8).overlap(2, chorus)`.
The chain carries on from whichever of the two ends later.
